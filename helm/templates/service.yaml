apiVersion: v1
kind: Service
metadata:
  name: {{ template "fullname" $ }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "fullname" $ }}
spec:
  clusterIP: None
  ports:
    - port: 27017
      name: mongo
  selector:
    app: {{ template "fullname" $ }}

